<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.win.app.product.ProductDAO">  
  
  <select id="getList" resultType="com.win.app.product.ProductDTO">
   SELECT * FROM PRODUCT
  </select>
  
  
  
  <select id="getDetail" resultType="ProductDTO" parameterType="ProductDTO">
  	SELECT * FROM PRODUCT WHERE PRODUCT_ID = #{product_id}
  </select>
  
  <insert id="addProduct" parameterType="ProductDTO">
  INSERT INTO PRODUCT (PRODUCT_ID, PRODUCT_TYPE, PRODUCT_RATE, PRODUCT_DETAIL) VALUES (PRODUCT_SEQ.NEXTVAL, #{product_type}, #{product_rate}, #{product_detail})
  </insert>
  
  <update id="updateProduct" parameterType="ProductDTO">
        UPDATE PRODUCT SET PRODUCT_TYPE = #{product_type}, PRODUCT_RATE = #{product_rate}, PRODUCT_DETAIL = #{product_detail} 
        WHERE PRODUCT_ID = #{product_id}
  </update>
  
  <delete id="deleteProduct" parameterType="ProductDTO">
        DELETE FROM PRODUCT WHERE PRODUCT_ID = #{product_id}
  </delete>
  
  </mapper>